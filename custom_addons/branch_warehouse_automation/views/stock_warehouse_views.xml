<?xml version="1.0" encoding="utf-8"?>
<odoo>

<record id="view_stock_warehouse_search" model="ir.ui.view">
    <field name="name">view.stock.warehouse.search</field>
    <field name="model">stock.warehouse</field>
    <field name="arch" type="xml">
        <search>
            <field name="name"/>
            <field name="company_id"/>
            <group expand="0" string="Group By">
                <filter string="Company" name="group_by_company" context="{'group_by':'company_id'}"/>
                <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
            </group>
        </search>
    </field>
</record>

    <!-- View stock.warehouse View Tree -->
    <record id="view_stock_warehouse_tree" model="ir.ui.view">
        <field name="name">view.stock.warehouse.tree</field>
        <field name="model">stock.warehouse</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!-- View stock.warehouse form -->
    <record id="view_stock_warehouse_form" model="ir.ui.view">
        <field name="name">view.stock.warehouse.form</field>
        <field name="model">stock.warehouse</field>
        <field name="arch" type="xml">
            <form string="Stock Warehouse">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- View stock.warehouse search -->
    <record id="view_stock_warehouse_search" model="ir.ui.view">
        <field name="name">view.stock.warehouse.search</field>
        <field name="model">stock.warehouse</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action stock.warehouse -->
    <record id="action_stock_warehouse" model="ir.actions.act_window">
        <field name="name">Stock Warehouse</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">stock.warehouse</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Stock Warehouse.
            </p>
        </field>
    </record>

</odoo>
