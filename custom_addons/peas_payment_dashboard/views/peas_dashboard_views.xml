<odoo>
  <!-- PEAS Users list (tree + form) -->
  <record id="view_peas_users_tree" model="ir.ui.view">
    <field name="name">peas.users.tree</field>
    <field name="model">hr.employee</field>
    <field name="arch" type="xml">
      <tree create="false" edit="false" delete="false">
        <field name="name"/>
        <field name="job_id"/>
        <field name="department_id"/>
        <field name="company_id" groups="base.group_multi_company"/>
      </tree>
    </field>
  </record>

  <record id="view_peas_users_form" model="ir.ui.view">
    <field name="name">peas.users.form</field>
    <field name="model">hr.employee</field>
    <field name="arch" type="xml">
      <form create="false" edit="false" delete="false">
        <sheet>
          <field name="image_128" widget="image"/>
          <h1><field name="name"/></h1>
          <group>
            <field name="job_id"/>
            <field name="department_id"/>
            <field name="work_email"/>
            <field name="work_phone"/>
          </group>
          <footer>
            <button 
              name="action_view_transactions"
              type="object" 
              string="View Transactions" 
              class="oe_highlight"/>
          </footer>
        </sheet>
      </form>
    </field>
  </record>

  <record id="action_peas_users" model="ir.actions.act_window">
    <field name="name">PEAS Users</field>
    <field name="res_model">hr.employee</field>
    <field name="view_mode">tree,form</field>
    <field name="domain">[('is_peas','=',True)]</field>
    <field name="context">{'create': False}</field>
  </record>

  <menuitem id="menu_peas_dashboard_root" name="PEAS Dashboard" sequence="80"/>
  <menuitem id="menu_peas_users" name="Users" parent="menu_peas_dashboard_root" action="action_peas_users" sequence="1"/>
</odoo>